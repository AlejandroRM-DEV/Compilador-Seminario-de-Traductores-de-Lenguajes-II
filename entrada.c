/*
Alejandro Ramirez Munoz * :D
*/
/*
Otro comentario :P
*/

int calcArea( int base, int altura ) {
    int area;
    if ( base > 0 && altura > 0 ) {
        area = base * altura;
    } else {
        area = 0;
    }
    return area;
}

int main() {
    int i, area;
    int base = 10, altura = 20;
    area = calcArea( base, altura );

    if( area > 100 ) {
        //Algo de codigo aqui
    } else {
        //Algo de codigo aqui
    }
    for( i = 0; i < 10; i++ ) {
        //Algo de codigo aqui
    }
    do {
        i = i - 1;
    } while( i >= 0 );
}
$
