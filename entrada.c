/*
Alejandro Ramirez Munoz * :D
*/
/*
Otro comentario :P
*/

int area(int base, int altura){
    int area;
    if (base >= 0 && altura >= 0 ){
        area = base * altura;
    } else {
        area = 0;
    }
    return area;
}

int main(){
    int i, area;
    int base = 10, altura = 20;
    area = area(base, altura);

    if( area > 100 ){
	//Algo de codigo aqui
    } else {
	//Algo de codigo aqui
    }
    for( i = 0; i < 10; i = i + 1 ){
	//Algo de codigo aqui
    }
    do{
	i = i - 1;
    }while( i >= 0);
}
$
